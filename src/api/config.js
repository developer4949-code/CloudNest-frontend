// API Configuration
export const API_BASE_URL = 'http://localhost:8080';

// Exporting individual service prefixes for flexibility
export const AUTH_BASE = `${API_BASE_URL}/api/auth`;
export const DOCUMENT_BASE = `${API_BASE_URL}/api/documents`;
export const REVIEW_BASE = `${API_BASE_URL}/review`;
